const ROI_THRESHOLDS = {
  good: 30,
  normal: 15,
  bad: 0,
};

const DEFAULT_AMAZON_FEE = 3.5;

const DEFAULT_REFERRAL_FEE_PERCENT = 15;

module.exports = {
    ROI_THRESHOLDS,
    DEFAULT_AMAZON_FEE,
    DEFAULT_REFERRAL_FEE_PERCENT,
};